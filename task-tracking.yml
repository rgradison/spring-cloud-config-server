spring:
  application:
    name: task-tracking

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,env,info,prometheus,refresh
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: task-tracking

logging:
  level:
    com:
      omnipotent:
        task_tracking: INFO